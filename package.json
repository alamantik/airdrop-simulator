{
  "name": "airdrop-simulator",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:light": "node build.mjs --light",
    "build:min": "node build.mjs --min",
    "build:pages": "node build.mjs --pages",
    "build:pages:min": "node build.mjs --min --pages",
    "test:smoke": "node smoke-test.mjs",
    "test:security": "node security-test.mjs",
    "test": "npm run build && npm run test:smoke && npm run test:security"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "playwright": "^1.49.1",
    "terser": "^5.39.0"
  }
}
